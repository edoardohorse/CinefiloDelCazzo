{
  "name": "@cinefilodelcazzo/stack",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build:types": "npm run build --workspace=types",
    "build:backend": "cp .env backend/.env && npm run build --workspace=backend",
    "build:frontend": "npm run build --workspace=frontend",
    "build": "npm run build --workspaces",
    "copy_env": "cp .env backend/.env",
    "start:backend": "npm run start --workspace=@cinefilodelcazzo/backend",
    "start:frontend": "npm run dev --workspace=@cinefilodelcazzo/frontend",
    "start": "concurrently \"npm:start:backend\" \"npm:start:frontend\""
  },
  "workspaces": [
    "types",
    "backend",
    "frontend"
  ],
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
